# Next Post Slate

date: 2026-05-24
decision: carousel-first, with static 9:16 companion assets from the same idea

## Priority 1 - Carousel Post

Title:

> Wallet Audit Love

First slide:

```text
Some wives don't ask. They audit wallets.
```

Why this ships:

- it no longer waits for a Reel signal;
- it is the main post;
- the vertical `9:16` set is generated from the same slide copy and scene logic;
- no Reel script, audio plan, beat sheet, or video treatment is required.

Status:

> GO for carousel-first generation.

Active package:

> `output/carousels/2026-05-24/wallet-audit-love-2/`

## Brand IP Lane

Series:

> One Instigator, One Alibi

Definition:

> One partner starts tiny nonsense. The other does not become an audience; they
> become the alibi.

Brand-fit categories:

- UPI / payments;
- wallets / cards / fintech;
- food delivery;
- q-commerce;
- shopping apps;
- travel and hotel spends;
- snacks and desserts.

Brand insertion principle:

```text
Product = receipt.
Relationship truth = premise.
```

## Hard Deadline Execution Order

1. Use `wallet-audit-love-2` as the active package.
2. Generate six native `4:5` carousel slides from `codex-image-prompts/instagram-post/`.
3. Generate six separate native `9:16` slides from `codex-image-prompts/reels-stories/`.
4. Package generated outputs into `final/` and `final-reels-stories/`.
5. Run visual QA for identity, text, aspect ratio, and safety/taste.
6. Post the `4:5` carousel.
7. Use the `9:16` set as the vertical companion asset; no Reel script required.
